version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: pdf-exporter
    ports:
      - "${PORT}:${PORT}"
    environment:
      PORT: ${PORT}
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev